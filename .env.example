# Copy to .env.local and set your key (never commit .env.local)
# 复制为 .env.local 并填写密钥（切勿提交 .env.local）
OPENAI_API_KEY=your_api_key_here

# Optional — defaults to gpt-4o-mini
# 可选 — 默认为 gpt-4o-mini
# OPENAI_MODEL=gpt-4o-mini
