/** Minimum pasted characters required to generate a paper card. */
export const MIN_PAPER_TEXT_LENGTH = 100;

/** Maximum pasted characters accepted by the API. */
export const MAX_PAPER_TEXT_LENGTH = 80_000;

/** Maximum user background characters accepted by the API. */
export const MAX_USER_BACKGROUND_LENGTH = 8_000;

/** Minimum user background for fit analysis + outreach email generation. */
export const MIN_USER_BACKGROUND_LENGTH = 80;
